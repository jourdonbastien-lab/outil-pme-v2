'use strict';
const assert=require('assert'); const parser=require('./lib/ebpQuoteParser'); const {createEbpParserHelpers}=require('./lib/ebpParserHelpers'); const {createEbpDocumentParserService}=require('./services/ebpDocumentParserService');
const normalizeSearchText=value=>String(value||'').toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/[^a-z0-9]+/g,' ').trim();
const parserHelpers=createEbpParserHelpers({normalizeSearchText,roundAmount:n=>Math.round(n*100)/100});
const service=createEbpDocumentParserService({parseEbpQuoteText:parser.parseEbpQuoteText,parseEbpInvoiceText:parser.parseEbpInvoiceText,parserHelpers});
const quoteText='DEVIS N° D-42\nClient : Dupont\nDate : 03/08/2026\nObjet : Portail\nTOTAL HT 1 000,00 €\nTOTAL TTC 1 200,00 €'; const invoiceText='FACTURE N° FA2026001\nClient : Dupont\nDate 03/08/2026\nTOTAL HT 100,00\nTVA 20,00\nTOTAL TTC 120,00';
const quote=service.parseQuote(quoteText),invoice=service.parseInvoice(invoiceText);assert.strictEqual(quote.parserName,'Analyse générique');assert.strictEqual(quote.quote_number,'D-42');assert.strictEqual(quote.quote_date,'2026-08-03');assert.strictEqual(quote.client_name,'Dupont');assert.strictEqual(quote.amount_ht,1000);assert.strictEqual(quote.amount_ttc,1200);assert.strictEqual(invoice.invoice_number,'FA2026001');assert.strictEqual(invoice.invoice_date,'2026-08-03');assert.strictEqual(invoice.client_name,'Dupont');assert.strictEqual(invoice.amount_ht,null);assert.strictEqual(invoice.vat_amount,null);assert.strictEqual(invoice.amount_ttc,120);assert.deepStrictEqual(Object.keys(service.parseQuote('')),['recognized','matched','reason','markersFound','markersMissing','inputLength','primaryTextLength','diagnostic','analysisUsed','parserName','client_name','amount_ht','amount_ttc','quote_number','quote_date','title']); console.log('OK - service parser EBP');
