'use strict';
const assert=require('assert'),fs=require('fs'),{createDashboardWeatherController}=require('./controllers/dashboardWeatherController');
async function run(){let result={statusCode:200,body:{ok:true}},calls=0;const c=createDashboardWeatherController({weatherService:{async getWeather(){calls++;return result}}});const res={code:200,status(n){this.code=n;return this},json(body){this.body=body;return this}};await c.getWeather({},res);assert.strictEqual(res.code,200);assert.deepStrictEqual(res.body,{ok:true});result={statusCode:503,body:{ok:false,error:'Météo indisponible'}};await c.getWeather({},res);assert.strictEqual(res.code,503);assert.strictEqual(calls,2);const source=fs.readFileSync('controllers/dashboardWeatherController.js','utf8');assert(!/fetch\(|open-meteo/.test(source));console.log('OK - contrôleur météo Dashboard')}run().catch(e=>{console.error(e);process.exitCode=1});
