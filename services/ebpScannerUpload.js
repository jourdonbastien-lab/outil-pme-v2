'use strict';
function createEbpScannerUpload({multer,path,scanDir,ensureDir,safeSegment,allowedExtensions,allowedMime,maxFileSizeBytes}){const storage=multer.diskStorage({destination(req,file,cb){try{ensureDir(scanDir);cb(null,scanDir)}catch(error){cb(error)}},filename(req,file,cb){const original=String(file.originalname||'scan'),ext=path.extname(original).toLowerCase(),safeBase=safeSegment(path.basename(original,ext)||'scan-ebp'),safeExt=allowedExtensions.has(ext)?ext:'.bin';cb(null,`${Date.now()}-${safeBase}${safeExt}`)}});return multer({storage,limits:{fileSize:maxFileSizeBytes},fileFilter(req,file,cb){const ext=path.extname(String(file.originalname||'')).toLowerCase(),mime=String(file.mimetype||'').toLowerCase();if(allowedExtensions.has(ext)||allowedMime.has(mime))return cb(null,true);cb(new Error('Format non supporté. Utilisez JPG, PNG, HEIC ou PDF.'))}});}
module.exports={createEbpScannerUpload};
