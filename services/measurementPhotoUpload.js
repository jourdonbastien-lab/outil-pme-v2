'use strict';
function createMeasurementPhotoUpload(d){const{multer,db,parseOptionalId,photoFiles,photoDir,ensureDir}=d;const storage=multer.diskStorage({destination(req,file,cb){try{const id=parseOptionalId(req.params.id);if(!id)return cb(new Error('ID fiche invalide'));const row=db.prepare('SELECT id, module FROM measurements WHERE id = ?').get(id);if(!row||row.module==='Escalier V2')return cb(new Error('Fiche de mesure classique introuvable'));const dir=photoFiles.measurementPhotoDir(photoDir,id);ensureDir(dir);cb(null,dir)}catch(e){cb(e)}},filename(req,file,cb){try{cb(null,photoFiles.generatedStoredName(file))}catch(e){cb(e)}}});return multer({storage,limits:{fileSize:photoFiles.MAX_FILE_SIZE,files:20},fileFilter(req,file,cb){try{photoFiles.validatePhotoFile(file);cb(null,true)}catch(e){cb(e)}}});}
module.exports={createMeasurementPhotoUpload};
