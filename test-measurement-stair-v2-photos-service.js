'use strict';const assert=require('assert'),Database=require('better-sqlite3'),{createMeasurementStairV2PhotosService}=require('./services/measurementStairV2PhotosService');const db=new Database(':memory:');db.exec('CREATE TABLE measurements(id INTEGER PRIMARY KEY,data TEXT,updated_at TEXT)');const row={id:1,data:JSON.stringify({fields:{legacy:'keep',photo_slots:[]}})};db.prepare('INSERT INTO measurements VALUES(1,?,NULL)').run(row.data);const normalizeSlots=v=>Array.isArray(v)&&v.length?v:[{category:'Autres',photos:[]}],s=createMeasurementStairV2PhotosService({db,fs:{existsSync:()=>false},path:require('path'),crypto:{randomUUID:()=> 'p1'},parseMeasurementData:JSON.parse,normalizeCategory:()=> 'Autres',normalizeSlots,buildPublicSlots:(id,slots)=>slots,photoBaseDir:()=>'/tmp',safeResolveInside:(...p)=>p.join('/'),ensureDir(){},now:()=> 'NOW'});const slots=s.add(row,'Autres',[{filename:'a.jpg',size:2,mimetype:'image/jpeg'}]);assert.strictEqual(slots[0].photos[0].id,'p1');const saved=JSON.parse(db.prepare('SELECT data FROM measurements WHERE id=1').get().data);assert.strictEqual(saved.fields.legacy,'keep');db.close();console.log('OK - photos Escalier V2');
